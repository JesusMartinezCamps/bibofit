// Empty service worker file for vite-plugin-pwa
// This file is used as a placeholder if injectRegister is set to 'auto' or 'script'
// and allows the browser to register a service worker.
// The actual service worker logic is generated by vite-plugin-pwa during build.

self.addEventListener('message', (event) => {
  if (event.data && event.data.type === 'SKIP_WAITING') {
    self.skipWaiting();
  }
});